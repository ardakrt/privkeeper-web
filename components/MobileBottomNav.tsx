"use client"; import { usePathname } from"next/navigation"; import Link from"next/link"; import { motion } from"framer-motion"; import { StickyNote, Wallet, Receipt, Bell, Timer, Shield, } from"lucide-react"; const navigationItems = [ { name:"Notlar", href:"/dashboard/notes", icon: StickyNote }, { name:"Cüzdan", href:"/dashboard/wallet", icon: Wallet }, { name:"Abonelikler", href:"/dashboard/subscriptions", icon: Receipt }, { name:"Hatırlatıcılar", href:"/dashboard/reminders", icon: Bell }, ]; export default function MobileBottomNav() { const pathname = usePathname(); return ( <nav className="fixed bottom-0 left-0 right-0 z-50 md:hidden bg-white/80 bg-black/80 backdrop-blur-xl border-t border-white/10 pb-safe"> <div className="flex items-center justify-around px-2 py-2"> {navigationItems.map((item) => { const isActive = pathname === item.href; const Icon = item.icon; return ( <Link key={item.href} href={item.href} className="flex-1"> <div className={` relative flex flex-col items-center justify-center py-2 px-3 rounded-xl transition-all duration-300 ${isActive ?"text-emerald-600 text-emerald-500" :"text-zinc-400 hover:text-zinc-900 hover:text-white" } `} > {isActive && ( <motion.div layoutId="mobile-active-bg" className="absolute inset-0 rounded-xl bg-emerald-100 bg-emerald-500/10" transition={{ type:"spring", bounce: 0.2, duration: 0.6 }} /> )} <Icon size={20} strokeWidth={isActive ? 2.5 : 2} className="relative z-10" /> <span className={`relative z-10 text-[10px] font-medium mt-1 ${isActive ? 'font-bold' : ''}`}> {item.name} </span> </div> </Link> ); })} </div> </nav> ); } 